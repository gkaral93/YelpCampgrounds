<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Campground</title>
</head>

<body>
    <h1>
        <%= campground.title %>
    </h1>
    <!--Using method ovveride -->
    <form action="/campgrounds/<%= campground._id%>?_method=PUT" method="POST">

        <div>
            <label for="inp">Title: </label>
            <input type="text" name="campground[title]" id="inp" value="<%= campground.title %> ">
        </div>
        <div>
            <label for="loc">Location: </label>
            <input type="text" name="campground[location]" id="loc" value="<%= campground.location %> ">
        </div>
        <p></p>

        <button>Update Campground</button>
        <br>
    </form>
    <footer>
        <p></p>
        <a href="/campgrounds/<%=  campground._id%>"><button>Back to Campground</button>
        </a>
    </footer>
</body>

</html>